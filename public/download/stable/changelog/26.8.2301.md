# 26.8.2301

## 修复
- 改进 zstd.dll 加载失败诊断，并确保发布包使用构建目录中的 DLL。
- 修复导出事务在文件重命名导致 Windows 文件 ID 改变时的恢复误判。
- 放宽预览导出速度上限至 1024 倍，并补充相关可靠性测试。

## 性能
- 优化统一输出尺寸封装：同尺寸帧直接复用像素缓冲，缩放路径复用输出缓冲并缓存帧索引，降低逐帧转换和重复读取的开销。
