# 26.9.1610

## 完善
- 主动扫描键位校验放宽：裸字母/数字等单键绑定不再直接拒绝，改为低优先级候选；同一动作绑定多个键位时优先使用组合键。
- 主动扫描发送快捷键前自动临时关闭 SAI2 输入法并在发送后恢复，避免单键被输入法组合吞掉。
- 等待 history 节点超时后自动重发快捷键一次；仍失败时错误信息附带前台窗口、焦点控件与 IME 状态诊断，便于定位按键未生效的原因。
