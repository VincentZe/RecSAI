# 发布迁移核对清单

本文档只供发版人核对，旧版客户端不会读取，也不是更新协议的一部分。

## 核对原则

- 应用内更新只保证升级到当前频道最新版本，不提供历史版本回退安装。
- 历史版本切换统一通过旧包下载完成，发版前必须确认 `changelog/index.json` 中的 `package_url` 对应文件真实存在。
- 遇到命名、主程序、缩略图扩展或安装结构变化时，优先在这里补充说明，再决定是否允许应用内直升。

## ReadSAI2 -> RecSAI 更名边界

### 26.2.2215 及更早

- 安装包前缀仍为 `ReadSAI2-*.zip`。
- 主程序为 `ReadSAI2.exe`。
- 缩略图扩展为 `ReadSAI2.ThumbnailProvider.dll`。
- 建议作为历史包下载项保留，不建议从更高版本执行应用内回退覆盖。

### 26.2.2223 过渡版本

- 安装包前缀切换为 `RecSAI-*.zip`。
- 主程序切换为 `RecSAI.exe`。
- 仍可能同时出现旧的 `ReadSAI2.ThumbnailProvider.dll` 与新的 `RecSAI.ThumbnailProvider.dll`。
- 这是首个必须重点核对的边界版本，发版前要确认旧文件残留与快捷方式行为。

### 26.3.102 及之后

- 安装包、主程序与缩略图扩展均统一使用 `RecSAI` 前缀。
- 若用户来自 `26.2.2215` 或更早版本，优先建议手动下载完整包覆盖。
- 若用户来自 `26.2.2223`，可按常规应用内更新验证，但仍需关注旧文件清理。

## 升级建议矩阵

| 来源版本 | 目标版本 | 建议方式 | 备注 |
| --- | --- | --- | --- |
| `26.2.2215` 及更早 | `26.2.2223` | 手动下载覆盖 | 涉及 `ReadSAI2.exe -> RecSAI.exe` 更名 |
| `26.2.2215` 及更早 | `26.3.102` 及更高 | 手动下载覆盖 | 同时核对缩略图扩展旧文件清理 |
| `26.2.2223` | 当前 stable 最新版 | 可应用内更新 | 发版前确认主程序与依赖替换链路 |
| `26.3.102` 及之后 | 当前频道最新版 | 可应用内更新 | 常规增量更新路径 |

## 发版前检查项

- `updates/download/<channel>/changelog/index.json` 已补齐 `version/date/title/package_name/package_url/manifest_url/is_latest`。
- `is_latest` 只标记当前频道最新一个版本。
- 对应 `changelog/<version>.md`、`package_url`、`manifest_url` 文件全部存在。
- `beta` 频道如果缺少日志目录或索引，直接视为发布不完整并拦截。
- 若涉及 EXE/DLL 更名，手动验证安装后目录中是否残留旧文件。
